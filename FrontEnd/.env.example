# Google OAuth Client ID
VITE_GOOGLE_CLIENT_ID=

# Backend service URLs for Vite dev-server proxy (local dev only)
VITE_PROXY_SPRING_URL=http://localhost:8080
VITE_PROXY_FASTAPI_URL=http://localhost:8000

# Direct API base URLs used by Axios (optional, leave empty to use current host proxy)
# VITE_API_URL=
# VITE_FASTAPI_URL=
